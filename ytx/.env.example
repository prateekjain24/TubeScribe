# Copy this file to `.env` and fill values as needed.

# Default engine: whisper | gemini
YTX_ENGINE=whisper

# Whisper (local) configuration
# Model choices commonly used: tiny, base, small, medium, large-v3, large-v3-turbo
WHISPER_MODEL=large-v3-turbo
# Device: cpu | auto | cuda | metal (Apple Silicon)
WHISPER_DEVICE=cpu
# Compute type for faster-whisper: auto | int8 | int8_float16 | float16 | float32
WHISPER_COMPUTE_TYPE=int8

# Gemini (cloud) configuration — required only when using --engine gemini
# Obtain API key from Google AI Studio and keep it secret.
GEMINI_API_KEY=
# Model examples: gemini-1.5-flash, gemini-1.5-pro (adjust per availability)
GEMINI_MODEL=gemini-1.5-flash

# Cache and output overrides (optional)
# Default cache directory is XDG (~/.cache/ytx)
YTX_CACHE_DIR=
# Default output dir is under the cache per job; can override
YTX_OUTPUT_DIR=

# Concurrency and timeouts (optional)
YTX_MAX_CONCURRENCY=3
YTX_HTTP_TIMEOUT=60

# yt-dlp cookies for restricted videos (optional)
# Use one of these if needed, but never commit cookie files.
YTDLP_COOKIES_FROM_BROWSER=  # e.g., chrome, brave, edge
YTDLP_COOKIES_FILE=
